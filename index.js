//a function expression called divide
const divide = function(){ 
  return 2000 / 100 
}

//arrow function called square
const square = num => num * num
  //takes one parameter and multiples it by itself
  //since only one parameter, no need for parentheses (num) or {}
  //does not need return

  //arrow function called add
  const add = (num1, num2) => num1 + num2;
     //doesnt need return because it's only one expession

